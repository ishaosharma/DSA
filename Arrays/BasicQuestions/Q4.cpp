Write a program to reverse the array.[Hint: use indexes] Here , 5 is the size of array and then elements of arrays are input

        // int n;
        // cout << "Enter size of array = " << endl;
        // cin >> n;
        // int arr[n];
        // cout << "Enter elements in array: " << endl;

        // for (int i = 0; i < n; i++){
        //     cin >> arr[i];
        // }

        // for (int i = n-1; i >= 0; i--)
        // {
        //    cout << arr[i] << " ";
        // }cout << endl;
